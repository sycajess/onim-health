export function AuthLoading() {
  return (
    <div
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: 'var(--teal-deep)',
        color: 'var(--teal-pale)',
        fontFamily: 'var(--font)',
        fontSize: 14,
      }}
    >
      Loading…
    </div>
  )
}
